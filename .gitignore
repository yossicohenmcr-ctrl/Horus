# Build artifacts
*.o
*.a
*.bin
*.elf
*.raw
*.iso
*.img
kernel.elf
boot.iso
horus.iso
.build.sha

# Rust build output
rust/target/

# newlib cross-toolchain (downloaded/built locally, not project source)
newlib/
*.deb

# rust/Cargo.lock is intentionally tracked for reproducible builds

# Build tools
tools/mkheadered

# Userspace build artifacts
userspace/*.bin
userspace/*.elf
userspace/*.o
userspace/*.raw

# Log and trace files
*.log
qemu.log
*.trace
*.dump
serial.log
debug.log

# Editor temporaries
*~
*.swp
*.swo
*.bak
*.tmp

# IDE
.vscode/
.idea/
*.sublime-project
*.sublime-workspace

# OS
.DS_Store
.AppleDouble
.LSOverride
Thumbs.db

# Secrets — never commit these
*.pem
*.key
*.crt
*.p12
id_rsa*
id_ed25519*
*.token
*.secret
.env
.env.local
.env.*.local
secrets/
credentials/
passwords.txt

# Personal notes
NOTES.md
TODO-personal.md
private/
personal/

# ISO staging directories
isofiles/
iso/
build/
dist/

# Windows artifacts
*.exe
*.dll
*.pdb

# AI editor artifacts
.aider*
.claude/
CLAUDE.md
graphify-out/

# System Prompt
system_prompt.txt

# Python
__pycache__/
*.pyc
.venv/
horus_usage.log

# Generated program-image byte arrays for PROC_SELFTEST (execve-from-fd)
userspace/*_image.h

# AI-assistant virtualenv (local dev only, never committed)
aider/
# extra reproducible-build hash sidecars
.build*.sha

# TLA+ model-checking: the pinned tla2tools.jar is downloaded (SHA-verified)
# by `make verify-tla`, never committed; TLC scratch output stays local too.
tla2tools.jar
docs/states/
docs/*.old
docs/MC_*
docs/*_TTrace_*.tla
